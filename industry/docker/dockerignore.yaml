schema_version: '1.0'
id: DOCKER-002
title: .dockerignore required
domain: docker
severity: medium
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Containerized templates must include .dockerignore to reduce build context and avoid copying secrets.
source_refs:
- docker.build.best-practices
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
  required_files:
  - .dockerignore
validation:
  kind: file_required
  required_files:
  - .dockerignore
matrix_builder:
  expose_in_report: true
  report_section: docker
  failure_message: Containerized templates must include .dockerignore to reduce build context and avoid copying secrets.
agent_generator:
  enforcement:
    starter: warn
    standard: warn
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
