schema_version: '1.0'
id: DOCKER-004
title: Container runtime should use non-root user
domain: docker
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Production container templates should avoid running as root at runtime.
source_refs:
- docker.build.best-practices
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: docker_non_root_user_check
matrix_builder:
  expose_in_report: true
  report_section: docker
  failure_message: Production container templates should avoid running as root at runtime.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
