schema_version: '1.0'
id: DOCKER-006
title: Secrets must not be copied into images
domain: docker
severity: critical
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Docker templates must not copy .env, credentials, SSH keys, or tokens into container images.
source_refs:
- docker.build.best-practices
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: docker_secret_copy_check
matrix_builder:
  expose_in_report: true
  report_section: docker
  failure_message: Docker templates must not copy .env, credentials, SSH keys, or tokens into container images.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
