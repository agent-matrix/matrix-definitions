schema_version: '1.0'
id: DOCKER-003
title: Use multi-stage builds for production images
domain: docker
severity: medium
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Production container templates should use multi-stage builds where applicable.
source_refs:
- docker.build.best-practices
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: docker_multistage_check
matrix_builder:
  expose_in_report: true
  report_section: docker
  failure_message: Production container templates should use multi-stage builds where applicable.
agent_generator:
  enforcement:
    starter: warn
    standard: warn
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
