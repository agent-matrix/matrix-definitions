schema_version: '1.0'
id: DOCKER-010
title: Package caches are not retained in image
domain: docker
severity: medium
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Generated Dockerfiles should clean package caches and avoid unnecessary build artifacts in runtime layers.
source_refs:
- docker.build.best-practices
applies_to:
  project_types:
  - fastapi-backend
  - fullstack-ai-app
  - api-service
  files: &id001
  - Dockerfile
generator:
  action: validate_only
  required_files: &id002
  - Dockerfile
  template_requirements:
  - no_package_cache_in_runtime
  files: *id001
validation:
  kind: docker_cache_policy
  required_files: *id002
matrix_builder:
  expose_in_report: true
  report_section: docker
  failure_message: Generated Dockerfiles should clean package caches and avoid unnecessary build artifacts in runtime layers.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
