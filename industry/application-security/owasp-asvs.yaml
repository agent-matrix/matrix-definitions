schema_version: '1.0'
id: ASVS-000
title: Use versioned ASVS references
domain: application-security
severity: medium
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Security reports should reference ASVS requirements using versioned identifiers when specific requirements are mapped.
source_refs:
- owasp.asvs
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: asvs_versioned_refs_check
matrix_builder:
  expose_in_report: true
  report_section: application-security
  failure_message: Security reports should reference ASVS requirements using versioned identifiers when specific requirements
    are mapped.
agent_generator:
  enforcement:
    starter: warn
    standard: warn
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
