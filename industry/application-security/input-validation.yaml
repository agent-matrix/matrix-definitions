schema_version: '1.0'
id: ASVS-001
title: Input validation required
domain: application-security
severity: critical
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: APIs and forms must validate untrusted inputs before processing or passing data to tools/agents.
source_refs:
- owasp.asvs
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: input_validation_required
matrix_builder:
  expose_in_report: true
  report_section: application-security
  failure_message: APIs and forms must validate untrusted inputs before processing or passing data to tools/agents.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
