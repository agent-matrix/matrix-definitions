schema_version: '1.0'
id: ASVS-005
title: Authentication must be explicit
domain: application-security
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Generated templates must not accidentally include partial or fake authentication; auth must be selected intentionally.
source_refs:
- owasp.asvs
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: auth_explicit_check
matrix_builder:
  expose_in_report: true
  report_section: application-security
  failure_message: Generated templates must not accidentally include partial or fake authentication; auth must be selected
    intentionally.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
