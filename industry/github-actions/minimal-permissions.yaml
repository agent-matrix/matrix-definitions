schema_version: '1.0'
id: GHA-001
title: GitHub Actions must use minimal token permissions
domain: github-actions
severity: critical
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Generated workflows must declare least-privilege permissions instead of relying on broad defaults.
source_refs:
- github.actions.secure-use
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
  workflow_requirements:
  - permissions declared at workflow or job level
validation:
  kind: github_actions_minimal_permissions
matrix_builder:
  expose_in_report: true
  report_section: github-actions
  failure_message: Generated workflows must declare least-privilege permissions instead of relying on broad defaults.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
