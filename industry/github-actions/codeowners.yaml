schema_version: '1.0'
id: GHA-003
title: CODEOWNERS required for workflow changes
domain: github-actions
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Repositories with workflows should include CODEOWNERS protection for workflow and definitions changes.
source_refs:
- github.actions.secure-use
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
  required_files:
  - CODEOWNERS
validation:
  kind: codeowners_required
matrix_builder:
  expose_in_report: true
  report_section: github-actions
  failure_message: Repositories with workflows should include CODEOWNERS protection for workflow and definitions changes.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
