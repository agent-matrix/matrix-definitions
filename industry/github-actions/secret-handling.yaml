schema_version: '1.0'
id: GHA-005
title: Workflow secrets must not be echoed or transformed unsafely
domain: github-actions
severity: critical
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Generated workflows must not print secrets and must avoid unsafe handling of untrusted input in shell scripts.
source_refs:
- github.actions.secure-use
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: github_actions_secret_handling
matrix_builder:
  expose_in_report: true
  report_section: github-actions
  failure_message: Generated workflows must not print secrets and must avoid unsafe handling of untrusted input in shell scripts.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
