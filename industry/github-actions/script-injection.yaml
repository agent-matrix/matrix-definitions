schema_version: '1.0'
id: GHA-006
title: Mitigate script injection in workflows
domain: github-actions
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Workflows must treat contexts from issues, PRs, and commit messages as untrusted and avoid direct shell interpolation.
source_refs:
- github.actions.secure-use
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: github_actions_script_injection_check
matrix_builder:
  expose_in_report: true
  report_section: github-actions
  failure_message: Workflows must treat contexts from issues, PRs, and commit messages as untrusted and avoid direct shell
    interpolation.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
