schema_version: '1.0'
id: AGENT-007
title: Agent eval cases recommended
domain: ai-agents
severity: medium
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Standard or higher agent templates should include golden cases or eval examples for key behavior.
source_refs:
- matrix.agent-safety.prompt-injection
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: agent_evals_present
matrix_builder:
  expose_in_report: true
  report_section: ai-agents
  failure_message: Standard or higher agent templates should include golden cases or eval examples for key behavior.
agent_generator:
  enforcement:
    starter: warn
    standard: warn
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
