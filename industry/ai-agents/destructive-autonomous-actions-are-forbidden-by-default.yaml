schema_version: '1.0'
id: AGENT-016
title: Destructive autonomous actions are forbidden by default
domain: ai-agents
severity: critical
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Generated agents must not perform destructive actions without explicit human approval and audit logging.
source_refs:
- matrix.agent-safety.human-approval
applies_to:
  project_types:
  - ai-agent
  - fullstack-ai-app
  files: &id001
  - agent/approval-policy.yaml
generator:
  action: render_file
  required_files: &id002
  - agent/approval-policy.yaml
  template_requirements:
  - destructive_action_approval_required
  files: *id001
validation:
  kind: human_approval_policy_present
  required_files: *id002
matrix_builder:
  expose_in_report: true
  report_section: ai-agents
  failure_message: Generated agents must not perform destructive actions without explicit human approval and audit logging.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
