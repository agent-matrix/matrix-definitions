schema_version: '1.0'
id: AGENT-004
title: Memory boundaries required
domain: ai-agents
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Agent templates with memory must document what can be stored, retrieved, expired, and audited.
source_refs:
- matrix.agent-safety.memory-safety
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: memory_boundary_required
matrix_builder:
  expose_in_report: true
  report_section: ai-agents
  failure_message: Agent templates with memory must document what can be stored, retrieved, expired, and audited.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
