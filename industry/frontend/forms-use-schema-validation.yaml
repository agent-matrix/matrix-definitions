schema_version: '1.0'
id: FE-006
title: Forms use schema validation
domain: frontend
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Generated forms must validate user input with a schema before submission to backend APIs.
source_refs: []
applies_to:
  project_types:
  - frontend-app
  - fullstack-ai-app
  files: &id001
  - frontend/
generator:
  action: render_form_policy
  template_requirements:
  - form_schema_validation_required
  files: *id001
validation:
  kind: frontend_form_schema_validation
matrix_builder:
  expose_in_report: true
  report_section: frontend
  failure_message: Generated forms must validate user input with a schema before submission to backend APIs.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
