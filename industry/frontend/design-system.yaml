schema_version: '1.0'
id: FE-002
title: Design tokens required
domain: frontend
severity: low
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Generated frontend templates should define reusable design tokens for spacing, colors, radius, typography, and shadows.[],
source_refs: []
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: design_tokens_present
matrix_builder:
  expose_in_report: true
  report_section: frontend
  failure_message: Generated frontend templates should define reusable design tokens for spacing, colors, radius, typography,
    and shadows.[],
agent_generator:
  enforcement:
    starter: warn
    standard: warn
    production: warn
    enterprise: warn
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
