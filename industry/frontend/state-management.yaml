schema_version: '1.0'
id: FE-004
title: State management must stay simple by default
domain: frontend
severity: low
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Generated MVP frontends should avoid unnecessary state libraries unless the blueprint explicitly requires them.
source_refs: []
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: state_management_simplicity_check
matrix_builder:
  expose_in_report: true
  report_section: frontend
  failure_message: Generated MVP frontends should avoid unnecessary state libraries unless the blueprint explicitly requires
    them.
agent_generator:
  enforcement:
    starter: warn
    standard: warn
    production: warn
    enterprise: warn
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
