schema_version: '1.0'
id: FASTAPI-005
title: FastAPI safe error responses required
domain: fastapi
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: FastAPI services should return safe errors and not leak stack traces or secrets to clients.
source_refs:
- fastapi.docs.security
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: safe_error_handling_required
matrix_builder:
  expose_in_report: true
  report_section: fastapi
  failure_message: FastAPI services should return safe errors and not leak stack traces or secrets to clients.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
