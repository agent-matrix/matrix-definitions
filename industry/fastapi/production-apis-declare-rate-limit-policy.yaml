schema_version: '1.0'
id: FASTAPI-013
title: Production APIs declare rate-limit policy
domain: fastapi
severity: medium
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Production and enterprise FastAPI templates must declare a rate-limit strategy for public write endpoints.
source_refs:
- owasp.asvs
applies_to:
  project_types:
  - fastapi-backend
  - fullstack-ai-app
  files: &id001
  - docs/security.md
generator:
  action: render_doc_section
  required_files: &id002
  - docs/security.md
  template_requirements:
  - rate_limit_policy_required
  files: *id001
validation:
  kind: required_section_present
  required_files: *id002
  required_sections:
  - Rate limiting
matrix_builder:
  expose_in_report: true
  report_section: fastapi
  failure_message: Production and enterprise FastAPI templates must declare a rate-limit strategy for public write endpoints.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
