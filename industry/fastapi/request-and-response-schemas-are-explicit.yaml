schema_version: '1.0'
id: FASTAPI-008
title: Request and response schemas are explicit
domain: fastapi
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Every generated FastAPI endpoint must use explicit request and response schemas to support validation and AI-coder acceptance criteria.
source_refs:
- fastapi.docs.security
- owasp.asvs
applies_to:
  project_types:
  - fastapi-backend
  - fullstack-ai-app
  files: &id001
  - backend/app/schemas/
generator:
  action: render_tree
  required_files: &id002
  - backend/app/schemas/
  template_requirements:
  - pydantic_schemas_required
  files: *id001
validation:
  kind: api_schema_required
  required_files: *id002
matrix_builder:
  expose_in_report: true
  report_section: fastapi
  failure_message: Every generated FastAPI endpoint must use explicit request and response schemas to support validation and AI-coder acceptance criteria.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
