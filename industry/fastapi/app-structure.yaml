schema_version: '1.0'
id: FASTAPI-001
title: FastAPI app structure must be modular
domain: fastapi
severity: medium
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: FastAPI templates should use a modular structure with API routes, schemas, services, and main app separated.
source_refs:
- fastapi.docs.security
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: fastapi_structure_check
matrix_builder:
  expose_in_report: true
  report_section: fastapi
  failure_message: FastAPI templates should use a modular structure with API routes, schemas, services, and main app separated.
agent_generator:
  enforcement:
    starter: warn
    standard: warn
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
