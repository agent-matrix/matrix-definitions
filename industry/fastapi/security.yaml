schema_version: '1.0'
id: FASTAPI-002
title: FastAPI security must be explicit
domain: fastapi
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: FastAPI templates must not include placeholder auth; security dependencies must be selected and documented explicitly.
source_refs:
- fastapi.docs.security
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: fastapi_security_explicit
matrix_builder:
  expose_in_report: true
  report_section: fastapi
  failure_message: FastAPI templates must not include placeholder auth; security dependencies must be selected and documented
    explicitly.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
