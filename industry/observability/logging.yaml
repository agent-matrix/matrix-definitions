schema_version: '1.0'
id: OBS-001
title: Structured logging recommended
domain: observability
severity: medium
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Backends should include structured logging guidance and avoid logging secrets.
source_refs:
- nist.ssdf.sp800-218
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: logging_guidance_present
matrix_builder:
  expose_in_report: true
  report_section: observability
  failure_message: Backends should include structured logging guidance and avoid logging secrets.
agent_generator:
  enforcement:
    starter: warn
    standard: warn
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
