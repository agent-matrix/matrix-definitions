schema_version: '1.0'
id: PY-008
title: Ruff lint configuration is present
domain: python
severity: medium
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Standard and higher Python templates must include lint configuration so generated code has deterministic quality checks.
source_refs:
- python.packaging.pyproject
applies_to:
  project_types:
  - python-agent
  - fastapi-backend
  - fullstack-ai-app
  files: &id001
  - pyproject.toml
generator:
  action: render_config
  required_files: &id002
  - pyproject.toml
  template_requirements:
  - ruff_config_required
  files: *id001
validation:
  kind: pyproject_section_required
  required_files: *id002
  required_sections:
  - tool.ruff
matrix_builder:
  expose_in_report: true
  report_section: python
  failure_message: Standard and higher Python templates must include lint configuration so generated code has deterministic quality checks.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
