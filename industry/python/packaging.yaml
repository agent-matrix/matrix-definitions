schema_version: '1.0'
id: PY-002
title: Python packaging metadata required
domain: python
severity: medium
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Python templates should include project metadata, dependencies, optional dev dependencies, and supported Python version.
source_refs:
- python.packaging.pyproject
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: python_packaging_metadata_check
matrix_builder:
  expose_in_report: true
  report_section: python
  failure_message: Python templates should include project metadata, dependencies, optional dev dependencies, and supported
    Python version.
agent_generator:
  enforcement:
    starter: warn
    standard: warn
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
