schema_version: '1.0'
id: PY-005
title: Python tests required
domain: python
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Generated Python backends and agents must include tests for health checks, schemas, and key services.
source_refs:
- python.packaging.pyproject
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
validation:
  kind: tests_required
matrix_builder:
  expose_in_report: true
  report_section: python
  failure_message: Generated Python backends and agents must include tests for health checks, schemas, and key services.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
