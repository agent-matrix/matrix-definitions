schema_version: '1.0'
id: PY-009
title: Type checking configuration is present
domain: python
severity: medium
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Generated Python backends should include a type checking policy so AI-coder patches can be validated consistently.
source_refs:
- python.packaging.pyproject
applies_to:
  project_types:
  - python-agent
  - fastapi-backend
  - fullstack-ai-app
  files: &id001
  - pyproject.toml
generator:
  action: render_config
  required_files: &id002
  - pyproject.toml
  template_requirements:
  - typing_config_required
  files: *id001
validation:
  kind: pyproject_section_required
  required_files: *id002
  required_sections:
  - tool.mypy
matrix_builder:
  expose_in_report: true
  report_section: python
  failure_message: Generated Python backends should include a type checking policy so AI-coder patches can be validated consistently.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
