schema_version: '1.0'
id: PY-001
title: Python projects use pyproject.toml
domain: python
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Python templates should use pyproject.toml as the central project configuration file.
source_refs:
- python.packaging.pyproject
applies_to:
  project_types:
  - all
generator:
  action: validate_and_report
  required_files:
  - pyproject.toml
validation:
  kind: file_required
  required_files:
  - pyproject.toml
matrix_builder:
  expose_in_report: true
  report_section: python
  failure_message: Python templates should use pyproject.toml as the central project configuration file.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
