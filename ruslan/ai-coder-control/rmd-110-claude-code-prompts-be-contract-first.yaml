schema_version: '1.0'
id: RMD-110
title: Claude Code prompts must be contract-first
domain: ruslan-ai-coder-control
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Claude Code prompt packs must begin with Matrix control files, active task scope, allowed files, forbidden
  changes, and validation commands.
source_refs: []
applies_to:
  project_types:
  - all
generator:
  action: generate_claude_code_prompt_policy
validation:
  kind: claude_code_prompt_policy_check
matrix_builder:
  expose_in_report: true
  report_section: ruslan-ai-coder-control
  failure_message: Claude Code prompt packs must begin with Matrix control files, active task scope, allowed files,
    forbidden changes, and validation commands.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
rationale: Ruslan Magana Definition used to make AI coders implement controlled tasks rather than acting as uncontrolled
  architects.
