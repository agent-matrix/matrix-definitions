schema_version: '1.0'
id: RMD-113
title: Gitpilot prompts must be Matrix-native and repository-scoped
domain: ruslan-ai-coder-control
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Gitpilot prompt packs must include Matrix Blueprint context, MatrixHub metadata, repository boundaries,
  and scoped task rules.
source_refs: []
applies_to:
  project_types:
  - all
generator:
  action: generate_gitpilot_prompt_policy
validation:
  kind: gitpilot_prompt_policy_check
matrix_builder:
  expose_in_report: true
  report_section: ruslan-ai-coder-control
  failure_message: Gitpilot prompt packs must include Matrix Blueprint context, MatrixHub metadata, repository boundaries,
    and scoped task rules.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
rationale: Ruslan Magana Definition used to make AI coders implement controlled tasks rather than acting as uncontrolled
  architects.
