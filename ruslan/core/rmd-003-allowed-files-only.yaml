schema_version: '1.0'
id: RMD-003
title: AI coders can edit only allowed files
domain: ruslan-core
severity: critical
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: AI-coder tasks must define allowed file paths, and validation must reject modifications outside that list.
source_refs: []
applies_to:
  project_types:
  - all
generator:
  action: create_allowed_changes_policy
  required_files:
  - MATRIX_ALLOWED_CHANGES.md
validation:
  kind: allowed_files_policy
matrix_builder:
  expose_in_report: true
  report_section: ruslan-core
  failure_message: AI-coder tasks must define allowed file paths, and validation must reject modifications outside that list.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
rationale: Ruslan Magana Definition used to keep AI-coder generation controlled, auditable, and repeatable.
control:
  enforce_allowed_files: true
