schema_version: '1.0'
id: RMD-005
title: Readable architecture documentation is mandatory
domain: ruslan-core
severity: high
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Every blueprint must include a human-readable architecture document explaining components, data flow, AI boundaries,
  and validation.
source_refs: []
applies_to:
  project_types:
  - all
generator:
  action: create_required_file
  required_files:
  - docs/architecture.md
validation:
  kind: file_required
  required_files:
  - docs/architecture.md
matrix_builder:
  expose_in_report: true
  report_section: ruslan-core
  failure_message: Every blueprint must include a human-readable architecture document explaining components, data flow, AI
    boundaries, and validation.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
rationale: Ruslan Magana Definition used to keep AI-coder generation controlled, auditable, and repeatable.
