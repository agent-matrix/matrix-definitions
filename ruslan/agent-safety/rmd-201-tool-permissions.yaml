schema_version: '1.0'
id: RMD-201
title: Every agent tool must declare permissions
domain: ruslan-agent-safety
severity: critical
status: stable
version: 1.0.0
owner: Ruslan Magana
created_at: '2026-06-12T00:00:00Z'
summary: Generated agents must include explicit tool permissions, risk level, inputs, outputs, and approval requirements.
source_refs: []
applies_to:
  project_types:
  - all
generator:
  action: create_tool_permissions
  required_files:
  - agent/permissions.yaml
validation:
  kind: tool_permissions_declared
matrix_builder:
  expose_in_report: true
  report_section: ruslan-agent-safety
  failure_message: Generated agents must include explicit tool permissions, risk level, inputs, outputs, and approval requirements.
agent_generator:
  enforcement:
    starter: warn
    standard: fail
    production: fail
    enterprise: fail
exceptions:
  allowed: true
  requires_expiry: true
  max_days: 90
rationale: Ruslan Magana Definition used to keep AI-coder generation controlled, auditable, and repeatable.
