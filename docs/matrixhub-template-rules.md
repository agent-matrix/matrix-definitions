# MatrixHub Template Rules

MatrixHub should accept only validated, documented, standards-aware templates.

A publishable template must include a blueprint contract, standards lock, README, standards report, artifact manifest, checksums, and provenance/signature metadata for production releases.
