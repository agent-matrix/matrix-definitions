# MATRIX_TASKS

This file is generated by Matrix Builder and consumed by AI coder adapters.

## Global rule

The AI coder is a worker, not the architect. It must implement only the assigned task and must not change the Matrix Blueprint, standards lock, stack, project type, quality level, or publication policy.

## Task format

Each task must include:

- task id
- purpose
- allowed files
- forbidden files
- acceptance criteria
- validation commands
- expected output format

## TASK-001 — Implement backend health and schemas

Purpose: create the minimal backend health check and report schema.

Allowed files:

- `backend/app/main.py`
- `backend/app/schemas/report.py`
- `backend/tests/test_health.py`

Forbidden changes:

- Do not modify `MATRIX_BLUEPRINT.yaml`.
- Do not modify `MATRIX_STANDARDS.lock`.
- Do not add authentication.
- Do not add external services.

Validation commands:

```bash
cd backend
pytest -q
```

Expected output: patch or scoped file edits only.
